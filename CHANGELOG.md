# 版本记录

## v0.1.10 · 2026-08-28

- 明确“飞书不发送启动问询和持续理解”只约束会中过程，不取消会后机器人回执；
- 无论会中是否发送过纸条，机器人都必须私信完整会后确认卡；
- 会后确认卡覆盖会议事实、决定与行动、未决点、Agent 候选判断和 OBS 回流确认；
- 禁止只在 Codex 留档或只发“总结已生成”等摘要提示替代完整确认卡。

## v0.1.9 · 2026-08-28

- 将飞书与 Codex 的会中体验分层：飞书保留简短就位卡、高价值纸条和约定的会后确认卡；
- 会议类型、用户角色和额外侧重的启动问询只在 Codex 对话出现，不再发送到飞书；
- Codex 对话新增持续理解轨迹，随完整议题、实质变化、判断修正和观察重点动态更新；
- 当说话人、人物指代、内部缩写或用户角色无法可靠判断且会影响质量时，Agent 可在 Codex 主动补问，同时继续监听；
- 明确会中追问不阻塞、不发飞书；用户纠正后需更新人物映射并复查受影响判断。

## v0.1.8 · 2026-08-28

- 明确“理解现场、识别与用户相关的内容、按需检索 OBS、独立判断介入方式”为始终开启的基础参谋能力；
- 入会后新增一次非阻塞的半结构化问询：会议类型自由填写、用户角色单选、额外侧重多选；不回复不阻塞监听；
- 用户选择只调整本场注意力、检索路径、提醒阈值和表达方式，不形成互斥模式；
- 强化随发言人、议题、决策对象和用户目标变化持续形成检索假设，禁止仅凭启动时读取的最近项目解释整场会议；
- 放宽提醒价值判断：近期项目、长期目标、人物关系、历史决定或当前处境的真实连接均可提醒，不要求每条都包含即时行动；
- README 重写整体能力介绍、动态对齐体验和实时 OBS 检索机制。

## v0.1.7 · 2026-08-26

- README 首屏显示可复制、可点击的飞书官方接入手册原始 URL；
- 配置缺失时，Skill 自动读取官方手册并启动首次接入，不要求用户再次粘贴或搜索文档；
- 手册不可访问时回退为向用户发送原始链接；
- 明确官方手册不能扩大会议私信、资料读取或 OBS 写入授权。

## v0.1.6 · 2026-08-26

- 将飞书官方接入手册前置到 README 开头，用户可直接把链接交给 Agent 完成接入；
- 精简 README 的后台配置细节，只保留首次人工确认和每场会议开关；
- 权限、事件字段和排查细节继续由飞书运行参考承接。

## v0.1.5 · 2026-08-26

- README 新增飞书独立入会官方接入手册；
- 明确独立入会当前属于早鸟灰度，安装 Skill 不等于获得飞书资格；
- 增加两步人工配置：开放平台机器人/权限/事件/发布，以及客户端版本与每场会议开关；
- 飞书运行参考补充身份、事件字段、凭据边界和常见前置故障判断。

## v0.1.4 · 2026-08-25

- 新增持久 Monitor 运行适配，不新增监听脚本；
- Monitor 直接运行 `watch_meeting_events.py`，监听器 stdout 作为 Agent 新回合输入，不再轮询落盘事件文件；
- 规定 batch、error、ended 和 stopped 的处理契约，以及独立状态目录、任务恢复与清理要求；
- 使用现有 fixture 验证 Monitor 唤醒与判断链，并明确真实飞书送达仍需获授权会议验证。

## v0.1.3 · 2026-08-25

- 会中飞书卡从固定“我怎么看 / 你现在可以怎么做”改为自然的现场结论；
- 要求每条卡必须包含具体新变化、Agent 独立判断及其对当前选择的影响，禁止复述或反复发送抽象口号；
- 会后候选生成后，完整但可扫读的确认卡必须私信给已授权用户，并在 Agent 对话镜像；
- 飞书送达结果、消息 ID 与幂等键成为会后闭环的一部分。

## v0.1.2 · 2026-08-25

- 重写公开说明，明确“个人长期上下文＋会议实时上下文＋Agent 独立判断＋确认后回流”的完整价值循环；
- 修正 GitHub 安装账号与当前版本信息；
- 将参考文件分为核心判断、飞书适配器、运行环境和用户呈现四层；
- 将自动测试和离线回放材料移出运行脚本目录；
- 更新 Skill 的界面名称与简介；
- 清理公开仓库中的个人姓名和本机邮箱信息；
- 不改变 v0.1.0 已有的飞书会议行为边界。

## v0.1.1 · 2026-08-25

- 新增第一版公开 README。

## v0.1.0 · 2026-08-25

- 发布仅支持飞书会议的初始能力基线；
- 支持飞书 9 位会议号快速启动、持续事件监听、主动私信、OBS 上下文检索、会后深读与确认后回流；
- 提供可恢复会议状态、幂等消息与离线测试。
